#include <stdio.h>
#include <conio.h>
int main(void)
   {
    printf("%d",num(20, 84));
    printf("\n%d",num(14, 50));
    printf("\n%d",num(11, 45));
    printf("\n%d",num(25, 40));
    }   
   int num(int x, int y)
        {
             return (x <= 20 || y >= 50) ||
			  (x <= 20 || y >= 50);

        }

